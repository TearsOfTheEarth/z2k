name: Build MT7628 (MIPS little-endian)

on:
  push:
    branches: [ mt7628-optimize ]
    paths:
      - 'mtproxy-client/**'
  workflow_dispatch:

jobs:
  build-mt7628:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./mtproxy-client
    
    steps:
    - uses: actions/checkout@v4
      with:
        ref: mt7628-optimize
    
    - name: Set up Go 1.25
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'
    
    - name: Build for MT7628 (mipsle softfloat)
      env:
        CGO_ENABLED: 0
        GOOS: linux
        GOARCH: mipsle
        GOMIPS: softfloat
      run: |
        go build \
          -ldflags="-s -w -X main.buildVersion=mt7628-$(date +%Y%m%d)" \
          -trimpath \
          -o tg-mtproxy-client-mt7628 \
          .
    
    - name: Check binary
      run: |
        file tg-mtproxy-client-mt7628
        ls -lh tg-mtproxy-client-mt7628
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: tg-mtproxy-client-mt7628
        path: mtproxy-client/tg-mtproxy-client-mt7628
        retention-days: 90
